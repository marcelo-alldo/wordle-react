import { styled } from '@mui/material/styles';
import { AppBar } from '@mui/material';

const StyledAppBar = styled(AppBar)(({ theme }) => ({
  marginBottom: '50px', // já estava funcionando
}));

export default StyledAppBar;
